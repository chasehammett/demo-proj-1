version: "3.9"
services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: appuser
      POSTGRES_PASSWORD: apppass
      POSTGRES_DB: appdb
    ports: ["5432:5432"]
    volumes: [dbdata:/var/lib/postgresql/data]
volumes: { dbdata: {} }
